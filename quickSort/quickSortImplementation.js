// ================ PIVOT HELPER FUNCTION ====================

function pivot(arr, start = 0, end = arr.length - 1) {

  // grab the pivot from the start of the array

  // store the current pivot index in a count variable(this will keep track of where the pivot should end up)

  // loop through the array from the start until the end
  
    // if the pivot is greater then the current array element, increment the pivot count variable and then swap the current element with the element at the pivot index

  // swap the starting element(i.e. the pivot) index

  // return the pivot index
};